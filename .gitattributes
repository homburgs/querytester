# Please read this: https://docs.github.com/en/github/using-git/configuring-git-to-handle-line-endings

# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Declare (explicit) files that will always have LF line endings on
*.sh   text eol=lf
*.env  text eol=lf
*.properties text eol=lf

# Declare (explicit) files that will always have CRLF line endings on checkout.
*.bat text eol=crlf
*.clv text eol=crlf
*.sln text eol=crlf

# Temporary, as are now in git repo, in feature we can all sources with LF line endings
*.java   text eol=lf
*.tml    text eol=lf
*.xml    text eol=lf
*.xaml   text eol=lf
*.cs     text eol=lf
*.txt    text eol=lf

# All files that are truly binary and should not be modified.
*.lyar binary
*.par -text

*.zip binary
*.jar binary

*.pdf binary
*.docx binary

*.ico  binary
*.png  binary
*.jpg  binary
*.jpeg binary

*.bin binary
*.dll binary
*.pdb -text
